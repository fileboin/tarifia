#!/usr/bin/env bash
# Cleanup Tarifia development environment when Conductor archives workspace
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

"$SCRIPT_DIR/cli/dev" docker cleanup --force
